<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="/admin/register.css">
</head>
<body>

<div class="logo-container">
    <img src="/admin/images/jlpuraskar.png" alt="Logo" class="logo">
</div>

<div class="register-box">
    <h2>Register</h2>
    <form action="/user/register" method="POST">

        <!-- Mobile Number (read-only and filled with session data) -->
        <input type="hidden" id="mobileNumber" name="mobileNumber" class="input-field" value="<%= mobileNumber %>" readonly />

        <!-- Full Name -->
        <div class="input-group">
            <label for="fullName">Full Name</label>
            <input type="text" id="fullName" name="fullName" required>
        </div>

        <!-- Date of Birth -->
        <div class="input-group">
            <label for="dob">Date of Birth</label>
            <input type="date" id="dob" name="dob" required>
        </div>

        <div class="input-group">
            <label for="user-type">User Type</label>
            <select id="user-type" name="userType" required>
              <option value="">Select Type</option>
              <option value="painter">Painter</option>
              <option value="contractor">Contractor</option>
              <option value="user">User</option>
              <option value="dealer">Dealer</option>
            </select>
          </div>

        <!-- Address -->
        <div class="input-group">
            <label for="address">Address</label>
            <input type="text" id="address" name="address" required >
        </div>

        <!-- Pin Code -->
        <div class="input-group">
            <label for="pincode">Pin Code</label>
            <input type="number" id="pincode" name="pinCode"required >
        </div>

        <!-- State Dropdown -->
        <div class="input-group">
            <label for="state">State</label>
            <input id="state" name="state" required>
        </div>

        <!-- City Dropdown -->
        <div class="input-group">
            <label for="city">City / District</label>
            <input id="city" name="city" required >
        </div>

        <!-- Register Button -->
        <button type="submit" class="button">Register</button>
    </form>
</div>

</body>
</html>
